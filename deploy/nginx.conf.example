# Beispiel: /etc/nginx/sites-available/wardogs-gateway
# TLS-Zertifikat z.B. mit certbot einrichten. HTTP ohne TLS NICHT für GUI-API-Keys verwenden.
server {
    listen 80;
    server_name rcon-gateway.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name rcon-gateway.example.com;

    ssl_certificate /etc/letsencrypt/live/rcon-gateway.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rcon-gateway.example.com/privkey.pem;

    client_max_body_size 128k;

    location / {
        proxy_pass http://127.0.0.1:9012;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_connect_timeout 5s;
        proxy_read_timeout 20s;
    }
}
